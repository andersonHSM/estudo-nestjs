import { UsuarioRequestExistenteGuard } from './usuario-request-existente.guard';

describe('UsuarioRequestExistenteGuard', () => {
  it('should be defined', () => {
    expect(new UsuarioRequestExistenteGuard()).toBeDefined();
  });
});
