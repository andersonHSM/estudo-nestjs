import { VisualizarApontamentosProvedorGuard } from './visualizar-apontamentos-provedor.guard';

describe('VisualizarApontamentosProvedorGuard', () => {
  it('should be defined', () => {
    expect(new VisualizarApontamentosProvedorGuard()).toBeDefined();
  });
});
