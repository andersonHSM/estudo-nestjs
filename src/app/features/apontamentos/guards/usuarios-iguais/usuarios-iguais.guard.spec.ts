import { UsuariosIguaisGuard } from './usuarios-iguais.guard';

describe('UsuariosIguaisGuard', () => {
  it('should be defined', () => {
    expect(new UsuariosIguaisGuard()).toBeDefined();
  });
});
